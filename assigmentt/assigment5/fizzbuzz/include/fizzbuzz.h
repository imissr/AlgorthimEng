#pragma once

#include <string>
#include <vector>

// Returns fizz/buzz/fizzbuzz or the number as a string.
std::string fizzbuzz_value(int i);

// Returns the fizzbuzz sequence from 1..n.
std::vector<std::string> fizzbuzz(int n);
